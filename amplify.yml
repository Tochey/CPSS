version: 1
frontend:
    phases:
        preBuild:
            commands:
                - echo "Running-----> preBuild"
        build:
            commands:
                - cd FE && npm install
                - cd FE && npm run build
    artifacts:
        baseDirectory: dist
        files:
            - "**/*"
    cache:
        paths:
            - node_modules/**/*
