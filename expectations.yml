CPSS EXPECTS:
    For 520 Students:
        - At most 1(ONE) problem description assignment (named problem description) available at a time under a coursename containing "capstone project spec" || "csc 520"(casing doesnt matter)(must be a txt only file upload)
        - At most 1(ONE) CSC520 Submission assignment (named csc520 submission) available at a time under a coursename containing "capstone project spec" || "csc 520"(casing doesnt matter)(must be a zip only file upload)
    For 521 Students:
